<Window x:Class="Pasyot_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pasyot_Launcher"
        mc:Ignorable="d"
        Title="Pasyot Launcher" Height="500" Width="850"
        MinHeight="450" MinWidth="750"
        Background="#181818" Foreground="White"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/app.ico">

    <Window.Resources>
        <Style x:Key="CustomScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Background="Transparent" BorderThickness="0" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Rectangle Fill="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Margin="0">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="rect" Background="#444444" CornerRadius="3"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="rect" Property="Background" Value="#007ACC"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter TargetName="rect" Property="Background" Value="#0098FF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Background="Transparent" BorderThickness="0" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Rectangle Fill="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Column="0" Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                       Grid.Column="1" 
                                       Cursor="Arrow" 
                                       Maximum="{TemplateBinding ScrollableHeight}" 
                                       Minimum="0" 
                                       Style="{StaticResource CustomScrollBar}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" 
                                       Value="{TemplateBinding VerticalOffset}" 
                                       ViewportSize="{TemplateBinding ViewportHeight}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="3"/>
                            <Rectangle x:Name="PART_Track" />
                            <Grid x:Name="PART_Indicator" ClipToBounds="True" HorizontalAlignment="Left">
                                <Border x:Name="Indicator" Background="{TemplateBinding Foreground}" CornerRadius="3"/>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LaunchButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0098FF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E1E1E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,10">
            <Border x:Name="ProfileArea"
                    Background="Transparent"
                    Cursor="Hand"
                    HorizontalAlignment="Left"
                    MouseLeftButtonUp="ProfileArea_MouseLeftButtonUp">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="40" Height="40" CornerRadius="20" Background="#252525">
                        <Ellipse x:Name="ProfileIcon" Width="36" Height="36" Fill="#007ACC"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Margin="12,0,0,0">
                        <TextBlock x:Name="ProfileText" Text="Sample Text" FontWeight="Bold" FontSize="14" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Popup x:Name="ProfileMenuPopup"
                   PlacementTarget="{Binding ElementName=ProfileArea}"
                   Placement="Bottom"
                   StaysOpen="False"
                   AllowsTransparency="True">
                <Border Background="#252525" BorderBrush="#333333" BorderThickness="1" CornerRadius="6" Padding="4" Margin="0,6,0,0">
                    <Button x:Name="LogoutButton"
                            Content="Выйти"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Height="35" Width="110"
                            Cursor="Hand"
                            Click="LogoutButton_Click"/>
                </Border>
            </Popup>

            <Button x:Name="SettingsButton"
                    Content="⚙ Настройки"
                    Style="{StaticResource SecondaryButtonStyle}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Height="35" Width="110"
                    Cursor="Hand"
                    Click="SettingsButton_Click"/>
        </Grid>

        <Grid Grid.Row="1" Margin="20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#202020" CornerRadius="8" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Pasyotpacks" Foreground="#888888" FontSize="11" FontWeight="Bold" Margin="5,0,0,8"/>

                    <ScrollViewer x:Name="ModpackList"
                                  Grid.Row="1"
                                  Style="{StaticResource CustomScrollViewer}"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="ModpackStackPanel" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
                    </ScrollViewer>

                    <TextBlock x:Name="EmptyModpacksText"
                               Grid.Row="1"
                               Text="Нажмите «+ Добавить pasyotpack»,&#x0a;"
                               Foreground="#666666"
                               FontSize="12"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               Margin="10"
                               Visibility="Collapsed"/>

                    <Button x:Name="AddModpackButton"
                            Grid.Row="2"
                            Content="+ Добавить pasyotpack"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Height="32"
                            Margin="0,8,0,0"
                            Cursor="Hand"
                            Click="AddModpackButton_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Margin="20,0,0,0">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Button x:Name="LaunchMinecraftBtn"
                            Content="ИГРАТЬ"
                            Style="{StaticResource LaunchButtonStyle}"
                            Height="55" Width="200"
                            Cursor="Hand"
                            Click="LaunchMinecraftBtn_Click"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button x:Name="OpenModpackFolderButton"
                                Content="Открыть папку"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Height="30" Width="120"
                                Cursor="Hand"
                                Click="OpenModpackFolderButton_Click"/>

                        <Button x:Name="VerifyFilesButton"
                                Content="Проверить файлы"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Height="30" Width="130"
                                Margin="8,0,0,0"
                                Cursor="Hand"
                                Click="VerifyFilesButton_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" Margin="20,0,20,15">
            <StackPanel x:Name="DownloadProgressPanel" Visibility="Collapsed" Margin="0,10">
                <TextBlock x:Name="StatusTextBlock" 
               Text="Загрузка..." 
               Foreground="White" 
               TextTrimming="CharacterEllipsis" 
               Margin="0,0,0,4"/>

                <ProgressBar x:Name="FileProgressBar" 
                 Height="10" 
                 Minimum="0" 
                 Maximum="100"/>

                <TextBlock x:Name="ProgressPercentTextBlock" 
               Text="0%" 
               Foreground="#A1A1AA" 
               HorizontalAlignment="Right" 
               Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
        <Frame x:Name="MainFrame" Content="Frame" Grid.RowSpan="3" NavigationUIVisibility="Hidden"/>

        <StackPanel x:Name="ToastHost"
                    Grid.RowSpan="3"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Margin="0,0,20,20"
                    Width="320"/>
    </Grid>
</Window>