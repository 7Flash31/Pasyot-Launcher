<UserControl x:Class="Pasyot_Launcher.Views.Pages.SkinPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="560" d:DesignWidth="700">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Скин" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>

        <Grid Grid.Row="1">

            <StackPanel x:Name="ConnectPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                <TextBlock Text="Не удалось подключиться к аккаунту" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,14"/>
                <Button x:Name="ConnectButton" Content="Повторить" Style="{StaticResource PrimaryButtonStyle}" FontSize="14" Height="42" Width="180" Click="ConnectButton_Click"/>
            </StackPanel>

            <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                <TextBlock Text="Загрузка..." Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" HorizontalAlignment="Center"/>
            </StackPanel>

            <Grid x:Name="EditorPanel" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="16" VerticalAlignment="Top" Margin="0,0,16,0">
                    <StackPanel>
                        <TextBlock Text="Текущий скин" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,10"/>
                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="8" Padding="8" HorizontalAlignment="Center">
                            <Image x:Name="SkinPreviewImage" Width="160" Height="160" Stretch="Uniform" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                        </Border>
                        <TextBlock x:Name="NoSkinText" Text="Скин ещё не установлен" Foreground="{StaticResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap" TextAlignment="Center" Margin="0,10,0,0" Visibility="Collapsed"/>
                        <TextBlock x:Name="SkinModelText" Text="Модель: classic" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="1">
                    <Border Style="{StaticResource CardBorderStyle}" Padding="16" Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock Text="Загрузить файл скина (PNG, 64×64)" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="FilePathTextBox"
                                         Grid.Column="0"
                                         Height="36"
                                         VerticalContentAlignment="Center"
                                         Background="{StaticResource SurfaceAltBrush}"
                                         Foreground="{StaticResource TextPrimaryBrush}"
                                         BorderBrush="{StaticResource BorderBrush}"
                                         BorderThickness="1"
                                         Padding="10,0"
                                         IsReadOnly="True"
                                         Text="Файл не выбран"/>
                                <Button Grid.Column="1" Content="Обзор..." Style="{StaticResource SecondaryButtonStyle}" Height="36" Width="90" Margin="10,0,0,0" Click="BrowseButton_Click"/>
                            </Grid>

                            <TextBlock Text="Модель рук" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                                <RadioButton x:Name="ClassicModelRadio" GroupName="Model" Content="Классика (Steve)" Foreground="{StaticResource TextPrimaryBrush}" IsChecked="True" Margin="0,0,20,0"/>
                                <RadioButton x:Name="SlimModelRadio" GroupName="Model" Content="Тонкие руки (Alex)" Foreground="{StaticResource TextPrimaryBrush}"/>
                            </StackPanel>

                            <Button x:Name="UploadButton" Content="Сохранить скин" Style="{StaticResource PrimaryButtonStyle}" FontSize="13" Height="40" Width="180" HorizontalAlignment="Left" IsEnabled="False" Click="UploadButton_Click"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardBorderStyle}" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Или укажите ник лицензионного аккаунта Minecraft" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="ImportUsernameTextBox"
                                         Grid.Column="0"
                                         Style="{StaticResource PlaceholderTextBoxStyle}"
                                         Tag="Ник"
                                         Height="36"/>
                                <Button Grid.Column="1" x:Name="ImportButton" Content="Импортировать" Style="{StaticResource SecondaryButtonStyle}" Height="36" Width="120" Margin="10,0,0,0" Click="ImportButton_Click"/>
                            </Grid>
                            <TextBlock Text="Скин применится при следующем запуске игры через лаунчер." Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
