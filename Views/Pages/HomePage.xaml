<UserControl x:Class="Pasyot_Launcher.Views.Pages.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="760">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">

            <Border Style="{StaticResource CardBorderStyle}" Padding="28" MinWidth="380" HorizontalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">

                    <Ellipse Width="72" Height="72" Fill="{StaticResource SurfaceAltBrush}" HorizontalAlignment="Center"/>

                    <TextBlock x:Name="PackNameText"
                               Text="Сборка не выбрана"
                               FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,14,0,4"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock x:Name="PackVersionText" Text="—" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>

                        <Border x:Name="LoaderBadge" Background="{StaticResource SurfaceAltBrush}" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" Visibility="Collapsed">
                            <TextBlock x:Name="LoaderBadgeText" Text="FABRIC" FontSize="9" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="ServerStatusPanel" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0" Visibility="Collapsed">
                        <Ellipse x:Name="ServerStatusDot" Width="7" Height="7" Fill="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock x:Name="ServerStatusText" Text="Проверка сервера..." Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button x:Name="LaunchButton"
                            Content="ИГРАТЬ"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Height="54" Width="220"
                            Margin="0,20,0,0"
                            Click="LaunchButton_Click"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                        <Button x:Name="OpenFolderButton" Content="Открыть папку" Style="{StaticResource SecondaryButtonStyle}" Height="32" Width="120" Click="OpenFolderButton_Click"/>
                        <Button x:Name="VerifyButton" Content="Проверить файлы" Style="{StaticResource SecondaryButtonStyle}" Height="32" Width="132" Margin="8,0,0,0" Click="VerifyButton_Click"/>
                    </StackPanel>

                    <TextBlock Text="Перетащите .pasyotpack сюда, чтобы добавить сборку" Foreground="{StaticResource TextMutedBrush}" FontSize="10" Margin="0,10,0,0" HorizontalAlignment="Center"/>

                    <Grid Margin="0,18,0,0">
                        <Button x:Name="RamChipButton" HorizontalAlignment="Center" Style="{StaticResource IconButtonStyle}" Width="Auto" Height="30" Padding="12,4" Click="RamChipButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ОЗУ:" Foreground="{StaticResource TextMutedBrush}" FontSize="11" VerticalAlignment="Center"/>
                                <TextBlock x:Name="RamChipText" Text="4096 МБ" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Popup x:Name="RamPopup" PlacementTarget="{Binding ElementName=RamChipButton}" Placement="Top" StaysOpen="False" AllowsTransparency="True">
                            <Border Style="{StaticResource CardBorderStyle}" Padding="16" Margin="0,0,0,10" Width="260">
                                <StackPanel>
                                    <Grid Margin="0,0,0,16">
                                        <TextBlock Text="Оперативная память" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" FontWeight="SemiBold"/>
                                        <TextBlock x:Name="RamPopupValueText" Text="4096 МБ" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Slider x:Name="RamPopupSlider" Style="{StaticResource CustomSliderStyle}" Minimum="1024" Maximum="16384" Value="4096" TickFrequency="512" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True" ValueChanged="RamPopupSlider_ValueChanged" Margin="0,0,0,4"/>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>

                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel x:Name="DownloadProgressPanel" Grid.Row="1" Visibility="Collapsed" Margin="0,16,0,0">
            <TextBlock x:Name="StatusTextBlock" Text="Загрузка..." Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,0,0,4"/>
            <ProgressBar x:Name="FileProgressBar" Style="{StaticResource ModernProgressBarStyle}" Minimum="0" Maximum="100"/>
            <TextBlock x:Name="ProgressPercentTextBlock" Text="0%" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" Margin="0,4,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
